import 'package:flutter/material.dart';

class ObservationFragment extends StatelessWidget {
  const ObservationFragment({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("观察"),
    );
  }
}